<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="EditTextHints">
        <item name="android:layout_marginTop">12dp</item>
        <item name="android:textColorHint">@color/colorPurpleLight</item>
        <item name="android:textColor">@color/colorPurpleThinLight</item>
        <item name="android:paddingStart">8dp</item>
        <item name="android:paddingEnd">8dp</item>
        <item name="android:paddingTop">10dp</item>
        <item name="android:paddingBottom">10dp</item>
    </style>

    <style name="CheckBoxTheme">
        <item name="android:buttonTint">#aa00c7</item>
    </style>

</resources>
